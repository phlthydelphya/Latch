You are @webrtc. For NETWORK TIMEOUT RESOLUTION with 2x headless browsers on 127.0.0.1:5173 and LiveKit 127.0.0.1:7880, confirm SFrame path will produce ciphertext in pcap: Encoded Transform primary + wasm-sframe fallback, sender_key = HKDF(epoch_secret, "sframe", sender_id), DataChannel HPKE, blind-forward 3 layers Last-N=9. Note that load-test.ts currently uses canvas.captureStream synthetic without SFrame — the 2x Playwright browser loop WILL exercise real SFrame. Provide checklist: how to verify rtp && sframe in pcap, KID/CTR varint, 16-byte auth tag, no plaintext NAL 00 00 00 01, and SFU opaque.
