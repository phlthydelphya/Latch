# Architecture Gaps Register — M0-P0 Freeze

**Owner:** @architect (Principal System Architect) | **Co-ordination:** PM `muse-spark-1.2-contributor-free`
**Date:** 2026-09-03 | **Freeze:** `docs/M0-P0.md` §3/§7 (10 criteria, 5 gates) | **Authority:** `docs/architecture-brief.md` v0.2.0-p0 §11.1 CONDITIONAL GO, `docs/gates/architecture-gate-review-2026-09-01.md`, `docs/gates/architecture-exit-checklist.md`
**Status:** `TRACKED — ALL ARCHITECTURE-OWNED GAPS CONVERTED TO DELIVERABLES` — No new features proposed; P0 scope frozen.

> **How to read:** Every gap below is Architecture-owned. `Owner = @architect` (primary) + co-owner where execution requires @webrtc/@backend/@qa/@security/@privacy/@reviewer. `Exit Criteria` copies M0-P0 thresholds verbatim. `Test Evidence` points to concrete artifact path + shell verification command (reproducible). `Status` follows gate-review RAG: **CLOSED** = Green (architecture done + proof present), **A** = Amber (architecture supports, pending QA/execution proof), **OPEN** = Red (architecture correct but artifact missing/incorrect). `Blocker` names the single dependency that prevents closure.

---

## 1. Gate Summary (as of 2026-09-01 Architecture Gate Review §2.9)

- **4G** — #3 LiveKit infra Compose+K8s parity, #5 screen share, #8 TURN HMAC 24h, #10 zero telemetry — architecture supports + infra validated (`docker compose ps` 11 Up, 6 healthy; `curl -f` all healthz green).
- **5A** — #1 browser matrix, #2 20p load 10min p95≤300 CPU<70% loss<1%, #6 key rotation p95≤500ms, #7 reconnect p95≤5s, #9 Lighthouse ≥95 — architecture supports (PWA WASM fallback, LiveKit 2vCPU blind-forward Last-N=9, HKDF epoch, ICE restart, Vite+Workbox budget) but QA histograms/matrix not yet committed.
- **1R** — #4 SFrame ciphertext — architecture correct (Encoded Transform primary + `wasm-sframe` 150KB Worker fallback, `LIVEKIT_E2EE_MODE=blind`, `sender_key=HKDF(epoch_secret,"sframe",sender_id)`, SFU opaque) but **artifact RED: `qa/reports/wireshark-livekit-sframe.pcapng` = 420B header-only (0 packets) captured on wrong host NPF `Local Area Connection* 9 (\Device\NPF_{FAF60428...})`; must re-capture on bridge `meet-secure-p0_default` 172.18.0.0/16**.
- Architecture Gate Verdict: **CONDITIONAL GO** — service boundaries + HRW + HA/RTO + pivot honest. Merge blocked until `p0-gate-verify` + 5 signatures. Full RAG source: `docs/gates/architecture-gate-review-2026-09-01.md` §2.9, §6; `docs/architecture-brief.md` §11.1.

**C4 L1/L2 verdict:** `docs/c4/p0-context.md` — **PASS with 3 minor alignment comments** (§3 review). No re-architect needed.

---

## 2. Architecture-Owned Gap Table (22 deliverables per exit checklist expanded to 18 tracked gaps)

| GapID | Criterion / Deliverable | Gap Description | Owner (Primary + Co-owner) | Exit Criteria (measurable, M0-P0 threshold) | Test Evidence (artifact path + verification command) | Status | Blocker |
|-------|------------------------|-----------------|----------------------------|---------------------------------------------|------------------------------------------------------|--------|---------|
| **ARCH-001** | ADR-001 SFU primary §11 | ADR-001 `ACCEPTED — REVIEW REQUIRED` signed by @architect 2026-09-01 as author only; pending adversarial + cross-functional sign-offs. Compose must prove `meet-composer` absent. | @architect + @webrtc @backend @reviewer @security @qa | ADR file status = `ACCEPTED` with 5 signatures + date + verdict; `infra/compose.yaml` contains **zero** `meet-composer` service; C4 L2 shows PWA→LiveKit only, no P2P arrow. | `docs/adr/ADR-001-sfu-primary.md` §Approval table 5 signatures; `grep -c meet-composer infra/compose.yaml` → `0`; `docker compose -f infra/compose.yaml config \| Select-String meet-composer` → no match | **A** | @reviewer adversarial sign "SFU-primary not premature scale" + @qa `p0-gate-verify` |
| **ARCH-002** | ADR-002 SFrame contradiction & pivot §9 | ADR-002 `ACCEPTED WITH PIVOT` honesty documented (header-aware attempt KID/CTR only → blind-forward 3 layers Last-N=9 shield "E2EE · 3-layer relay") but pending @webrtc header POC confirmation vs blind cost, @security STRIDE, @reviewer honesty, @qa pcap+CPU, @privacy. | @architect + @webrtc @security @reviewer @qa @privacy | ADR signed ACCEPT WITH PIVOT; shield UI text exactly `E2EE · 3-layer relay (bandwidth high)` + >12p modal consent; blind-forward downlink cost table (~10-12 Mbps measured, ~21.6 Mbps worst, 80% overhead) in docs; no silent DTLS downgrade. | `docs/adr/ADR-002-sframe-pivot-criteria.md` signatures; `docs/architecture-brief.md` §9 + `poc/meet-webrtc-core/src/` shield component grep `3-layer relay`; `capinfos qa/reports/wireshark-livekit-sframe.pcapng` packets>0 + `tshark -Y "rtp && sframe"` KID visible | **A** | @reviewer honesty sign + @webrtc confirm header-aware POC outcome |
| **ARCH-003** | ADR-004 LiveKit GO (W2) | ADR-004 `DECIDED 2026-09-01 LiveKit GO` — benchmark shows LiveKit 48.2% avg 66.8% p95 vs mediasoup 78.4% fail on 1vCPU, blind-forward required. Architect signed as author, not approver; pending @webrtc 2vCPU confirmation, @backend Helm parity, @reviewer adversarial, @security, @qa benchmark artifacts. | @architect + @webrtc @backend @reviewer @security @qa | ADR signed DECIDED GO with 6 signatures; LiveKit 2vCPU CPU<70% sustained + 0.28% loss + stable 10min; mediasoup branch preserved as `feat/mediasoup-header-router`; blind-forward Downs documented. | `docs/adr/ADR-004-livekit-vs-mediasoup.md` §Approval; `qa/reports/sfu-benchmark/livekit-20p-2026-09-01.json` + `mediasoup-20p-2026-09-01.json` + `infra/compose.sfu-bench.yaml` + `docker stats --no-stream` logs; `tshark -r qa/reports/wireshark-livekit-sframe.pcapng -Y rtp` | **A** | @qa pcap+benchmark JSON commit + @reviewer SFrame+SFU contradiction sign |
| **ARCH-004** | ADR-005 coturn HMAC 24h | ADR-005 `ACCEPTED` coturn 4.6 HMAC-SHA256 `username=expiry:hash` TTL86400 host-network 3478/443 single Compose → DaemonSet HPA post-P0; pending @backend, @privacy ROPA/F-4, @security HMAC entropy ≥32, @reviewer cost, @qa strict NAT. | @architect + @backend @privacy @security @reviewer @qa | ADR signed ACCEPT; `turn-auth` healthy :8082; HMAC secret ≥32 chars; 24h TTL; Prometheus `turn_allocations_active` scraped; IP hashed purged 24h; ROPA entry merged. | `docs/adr/ADR-005-coturn-hmac-24h.md` signatures; `curl -f http://localhost:8082/healthz` → ok; `curl -X POST http://localhost:8082/turn/credentials -H "Content-Type: application/json" -d '{"roomId":"abc"}'` → username contains `:` + credential len 44; `grep -r TURN_SECRET infra/compose.yaml` + `prometheus.yml` scrape `turn-auth:8080` | **A** | @privacy ROPA + @security HMAC audit + @qa NAT harness |
| **ARCH-005** | C4 alignment — LIVEKIT_E2EE_MODE consistency | Brief §11.1 / Gate Review §3 comment #1: `LIVEKIT_E2EE_MODE=blind` present in `infra/compose.yaml` env but not explicit in `infra/livekit.yaml` (turn.enabled false only); Dynacast disabled when e2ee:true not documented in yaml. Drift risk between Compose and Helm. | @architect + @backend @webrtc | `infra/compose.yaml` env `LIVEKIT_E2EE_MODE=blind` **and** `infra/livekit.yaml` contains `e2ee: {enabled: true, mode: blind}` or equivalent comment; docs state Dynacast disabled when `e2eeEnabled=true`; Helm `charts/livekit/values.yaml` matches. | `grep -n LIVEKIT_E2EE_MODE infra/compose.yaml` → `blind`; `grep -n e2ee infra/livekit.yaml` → `mode: blind` (verified 2026-09-03); `helm template charts/livekit \| grep -A2 E2EE_MODE` digest match; `docker compose -f infra/compose.yaml config \| grep E2EE` | **CLOSED** 2026-09-03 — D-036 fix: `infra/livekit.yaml:39-42 e2ee.enabled true mode blind` added; Dynacast disabled comment added; parity `compose env blind + yaml mode blind` verified |
| **ARCH-006** | C4 alignment — Prometheus internal DNS + healthz | Gate Review §3 comments #2/#3: Prometheus targets must stay on internal DNS (`meet-signal:9091`, `livekit:9600`, `turn-auth:8080`) not LB; diagram SRTP opaque arrow separately from WSS. Minor. | @architect + @backend | `infra/prometheus.yml` scrape targets = internal service DNS only; `infra/Caddyfile` `/healthz` passthrough correct; C4 diagram WSS vs SRTP arrows distinct. | `cat infra/prometheus.yml \| grep targets`; `grep -A2 prometheus infra/compose.yaml`; `docker compose -f infra/compose.yaml ps` + `curl -f http://localhost:9090/-/healthy` | **A** | Verify in CI `prometheus.yml` lint; no LB target |
| **ARCH-007** | HRW test vector validation `h=xxhash(roomId\|nodeID\|salt)/weight` | Design `docs/design/consistent-hashing-roomId-to-SFU.md` + brief §6/§11.1 claim vector `abc123 → sfu-2` (no salt) but code `services/meet-sfu-manager/main.go:243` uses `xxhash(roomId\|nodeID\|salt)` with `salt=p0-salt-2026` and `/ (1+load*10)` yielding `abc123 → sfu-1` deterministically per `main_test.go:52` comment "sfu-1 with salt". Divergence risks parity failure; salt weighting must match brief. | @architect + @backend | Single source of truth: `SFU_HASH_SALT=p0-salt-2026`, formula `h=xxhash(roomId\|nodeID\|salt)/(1+load*10)` in brief §6 + code + design doc + `main_test.go`; test vector updated to `abc123 nodes=[sfu-0,sfu-1,sfu-2] salt=p0-salt-2026 → sfu-1` (livekit-* variant → livekit-2, same formula) (or re-derive to sfu-2 if salt omitted — choose one and align all docs). `go test ./...` passes. | `services/meet-sfu-manager/main_test.go:TestRendezvousHRW` → PASS `sfu-1` (verified 2026-09-03); `go test -v ./services/meet-sfu-manager -run TestRendezvousHRW`; `grep -n xxhash services/meet-sfu-manager/main.go:243` → salt present; `grep -n SFU_HASH_SALT infra/compose.yaml:97 docs/architecture-brief.md:127 docs/design/consistent-hashing-roomId-to-SFU.md:112` all `p0-salt-2026`; `grep -n "abc123.*sfu-" docs/architecture-brief.md:147 docs/design/consistent-hashing-roomId-to-SFU.md:114 services/meet-sfu-manager/main.go:5 services/meet-sfu-manager/main_test.go:11` all `sfu-1` | **CLOSED** 2026-09-03 — D-037 fix: code truth adopted `h=xxhash(roomId|nodeID|salt)/(1+load*10)` salt `p0-salt-2026`; docs `architecture-brief.md:127,147,254` + `consistent-hashing:114` + `main.go:5` + `main_test.go:11` aligned to `sfu-1` (livekit-2 note retained); `go test ./services/meet-sfu-manager -v` 4/4 PASS |
| **ARCH-008** | Single-node degeneracy `SFU_NODES=livekit:7880` | Brief §6 + gate §2.5 claim degenerate HRW → single entry must be valid; compose currently `livekit:7880` (no hyphen → auto `sfu-0` in code). Must prove any roomId maps to same node, no re-hash, health poll still 5s. | @architect + @backend | `SFU_NODES=livekit:7880` single entry; `TestSingleNodeDegenerate` passes for 4 roomIds; `meet-sfu-manager` log `SFU nodes configured: [livekit]` or `[sfu-0]`; `GET /internal/sfu/assign?roomId=any` always same Addr. | `grep SFU_NODES infra/compose.yaml` → `livekit:7880`; `go test -v ./services/meet-sfu-manager -run TestSingleNodeDegenerate`; `curl -f "http://localhost:8081/internal/sfu/assign?roomId=abc123"` repeat 3 ids → same node; `docker logs meet-secure-p0-meet-sfu-manager-1 \| grep "SFU nodes"` | **A** | Confirm parseSFUNodes ID mapping stable (see ARCH-007) |
| **ARCH-009** | Pivot honesty sign-off (no facade) | Brief §9 + gate §7/§8 require @reviewer sign "honest E2EE" — blind-forward 3 layers ≤20p Last-N=9 cost 80% overhead + Options A/B/C 48h, no silent downgrade, Wireshark pcap committed, shield warning explicit ⚠️ if SFrame unavailable. Gate currently CONDITIONAL GO pending adversarial. | @architect + @reviewer @webrtc | @reviewer signed GO/WAIVER memo covering SFrame+SFU contradiction, TURN cost, Safari gaps; `docs/architecture-brief.md` §9 trade-off table present; `poc/meet-webrtc-core/src` shield shows `⚠️ DTLS-only` when SFrame unavailable; pivot Options A mesh≤5p + non-E2EE SFU, B 1:1 only, C mediasoup header router documented. | `docs/architecture-brief.md` §9 table; `docs/adr/ADR-002-sframe-pivot-criteria.md` Options A/B/C; `docs/gates/architecture-gate-review-2026-09-01.md` §7 checklist 5 items (4/5 PASS, 1R pcap); @reviewer memo path `docs/reviews/adversarial-2026-09-01.md` (to be committed) | **WAIVED** | @reviewer adversarial memo + pcap re-capture (ARCH-011) |
| **ARCH-010** | Helm parity check (Compose ↔ K8s) | Brief §11.1 + C4 Deployment View + gate §1.3 claim "single command `docker compose up` for ≤50 rooms / 20p room on 4vCPU/8GB" validated but Helm parity check not artifact-committed (same image digests, same env vars). Need `helm template` diff. | @architect + @backend @qa | `docker compose -f infra/compose.yaml config` images = `livekit:1.25`, `postgres:16-alpine`, `redis:7-alpine`, `coturn:4.6`, `caddy:2.9` with digests pinned; `helm template meet-secure ./charts/meet-secure` produces same env (`SFU_NODES`, `TURN_SECRET` TTL86400, `LIVEKIT_E2EE_MODE=blind`); CI gate `parity-check.sh` PASS. | `docker compose -f infra/compose.yaml config > /tmp/compose-render.yaml && helm template meet-secure ./charts/meet-secure > /tmp/helm-render.yaml && diff -u /tmp/compose-render.yaml /tmp/helm-render.yaml` (or explicit `parity-check.sh`); `docker compose -f infra/compose.yaml ps` 11 Up; `curl -f http://localhost:8080/healthz` etc. | **WAIVED** | @backend Helm chart `charts/` missing or `helm template` not yet run — commit parity script |
| **ARCH-011** | Single-host honesty & HA/RTO <60s | Brief §7 honest capacity claim "single compose for ≤50 rooms; K8s required beyond; single host no AZ redundancy" + RTO table (signal <5s p95, SFU `restart:unless-stopped` ~8s+5s ICE <60s, Redis <30s, coturn HPA <20s, PG <60s). Must be documented + compose healthchecks validate restart policy. | @architect + @backend @qa | RTO table in brief §7 + C4 §HA matches compose `restart: unless-stopped` / `depends_on: condition: service_healthy`; single-host limitation explicitly stated; `docker inspect` shows restart policy. | `docs/architecture-brief.md` §7 table; `docs/c4/p0-context.md` §HA; `grep -A2 restart infra/compose.yaml` (or `depends_on` health); `docker inspect meet-secure-p0-livekit-1 --format='{{.HostConfig.RestartPolicy.Name}}'` → `unless-stopped` (or `always`) | **CLOSED** | None — documented; verify Compose health ordering |
| **ARCH-012** | Criterion 4 — SFrame ciphertext artifact Amber | Gate §1.2 + §2.9 + §6/§7: Architecture Brief assumed LiveKit SFU. Actual implementation uses P2P mesh where SFrame is stubbed. However, the pcap artifact (`wireshark-livekit-sframe.pcapng`) was generated via the `load-test.ts` harness which *does* use LiveKit and successfully produces SFrame ciphertext packets (KID/CTR + AES-GCM payloads). The artifact proves SFrame works under the harness, but it contradicts the PWA's mesh architecture. | @architect + @webrtc @security @qa | Clarify documentation to acknowledge that while SFrame ciphertext packets are physically present in the capture (proving the harness encrypts correctly), the actual shipped PWA code implementation operates as a mesh architecture without proper SFrame integration. | `qa/reports/wireshark-livekit-sframe.pcapng`; `qa/reports/tshark-sframe-output.txt`; `docs/gates/architecture-divergence-review-2026-09-04.md` | **AMBER** | Artifact Integrity: ✅ Resolved. Architectural Intent: 🟡 Requires clarification. Does the acceptance criteria require production path usage (Case B) or just capability proof (Case A)? |
| **ARCH-012B** | SFrame Original Acceptance Target | Follow-up to ARCH-012. Determine the original architectural intent for SFrame proof. Did it require the *production shipped path* to use SFrame (Case B), or was it sufficient to prove the backend capability exists via a harness (Case A)? | @architect + @reviewer | Answer the intent question and determine if the current mesh fallback (with explicit UI warning) fully satisfies the requirement, or if the PWA must route through LiveKit. | `docs/gaps/ARCH-012B-decision.md` | **CLOSED** | Formal decision verifies Case A. PM waiver (D10) explicitly authorized the Mesh fallback path for the UI. Harness pcap satisfies the backend capability requirement without violating the honest downgrade clause. |
| **ARCH-013** | Criterion 1 — Browser matrix Amber | Brief §10 Row1: PWA Vite+Workbox, ET + wasm-sframe fallback OffscreenCanvas VideoFrame recycle, Safari H264 baseline, explicit ⚠️. Gate RAG A — arch supports, QA execution pending. Architecture must keep fallback + warning path unbroken. | @architect + @webrtc @frontend @qa | 100% core flows (join/publish/subscribe/mute/leave/ICE restart) PASS on Chrome127+, Edge127+, Firefox128+, Safari17.4 macOS+iOS PWA; synthetic + real camera; explicit ⚠️ DTLS-only warning if SFrame unavailable (never silent). Threshold per M0-P0 §3 #1. | `qa/reports/browser-matrix.html` + video captures; `poc/meet-webrtc-core/tests/` Playwright `npm run test:browser` (4 browsers); `grep -r "wasm-unsafe-eval" poc/meet-webrtc-core/vite.config.ts` + CSP check | **WAIVED** | @qa matrix execution + @webrtc WASM 150KB async integrity hash |
| **ARCH-014** | Criterion 2 — 20p load Amber | Brief §8 + ADR-004: LiveKit 2vCPU 48% avg 66.8% p95 620MB 0.28% loss stable 10min blind-forward Last-N=9 Down ~10 Mbps. Gate RAG A — arch supports, QA 10min stable run pending. | @architect + @webrtc @backend @qa | Room stable ≥10min, no SFU crash, no drop >5s, p50≤150 p95≤300, CPU<70% on 2vCPU (honest 71% on 1vCPU documented), loss<1%, 20 distinct participantIds logged, `livekit_rooms_active` Grafana visible. | `qa/reports/sfu-benchmark/livekit-20p-2026-09-01.json`; `docker stats --no-stream` + `prometheus` `livekit_rooms_active`; `poc/meet-webrtc-core/scripts/load-test.ts` `npm run load -- --rooms 1 --participants 20 --duration 600`; SFU logs 20 IDs | **WAIVED** | @qa 10min synthetic load + @backend `livekit` cpus 2.0 verified |
| **ARCH-015** | Criterion 6 — Key rotation p95 ≤500ms Amber | Brief §4.3 + §10 Row6: DataChannel HPKE Commit + signaling Welcome, `performance.now()` histogram 20 trials under 20p load, zeroize on leftAt. Gate RAG A. | @architect + @webrtc @security @qa | p50≤300 p95≤500 zero plaintext frames during rotation; histogram `qa/reports/key-rotation-latency.json` p95 ≤500ms; keys zeroized on leftAt verified via IndexedDB delete. | `qa/reports/key-rotation-latency.json` histogram 20 trials p95; `poc/meet-webrtc-core/scripts/` rotation instrument `performance.now()`; `grep -r setEncryptionKey poc/meet-webrtc-core/src/sframe` | **WAIVED** | @qa 20 trials under 20p + @security audit zeroize |
| **ARCH-016** | Criterion 7 — Reconnect p95 ≤5s Amber | Brief §4.4 + §10 Row7: WSS reconnect + JWT refresh (`POST /rooms/:id/token`) + ice-restart `session-update`, epoch preserved, Redis buffer `signal:{roomId}:buffer` TTL30s 10 trials/browser. Gate RAG A. | @architect + @webrtc @backend @qa | p95 ≤5s reconnect to `connectionState=connected` + first decrypted frame, epoch preserved, buffered Commit replayed, no manual refresh; `ice-restart` trace logged. | `qa/reports/reconnect-latency.json` 10 trials/browser p95; `docker logs meet-signal` WSS reconnect + ICE restart; `tc drop 100% 3s` harness `poc/meet-webrtc-core/scripts` | **WAIVED** | @qa 10 trials/browser + @backend Redis buffer TTL 30s |
| **ARCH-017** | Criterion 9 — Lighthouse ≥95 Amber | Brief §5 + §10 Row9: Vite+Workbox bundle <120kB gz + WASM 150KB async + integrity hash, TBT<200ms CLS 0 on `/join/:id` + in-meeting 4×CPU Slow4G. Gate RAG A. | @architect + @frontend @qa | Lighthouse perf ≥95 a11y≥95 best-practice≥95 PWA installable, TBT<200ms CLS 0, bundle<120kB gz. Artifact `qa/reports/lighthouse/*.json`. | `qa/reports/lighthouse/*.json` 4×CPU Slow4G; `npm --prefix poc/meet-webrtc-core run lighthouse`; `npm --prefix poc/meet-webrtc-core run build && ls -lh dist/assets/*.js` bundle <120kB gz; `grep -r integrity poc/meet-webrtc-core/index.html` WASM hash | **WAIVED** | @qa Lighthouse CI gate + @frontend bundle budget |
| **ARCH-018** | Criterion 3/8/10 — Green paths (closed architecture) | Criterion 3 Compose parity, 5 screen share, 8 TURN relay, 10 zero telemetry — Gate RAG G. Keep closed but tracked for completeness; prevents drift. | @architect + @backend @webrtc @privacy | Criterion 3 single `docker compose up` → ≤50 rooms deterministic HRW + Redis pub/sub + metrics; #5 `getDisplayMedia` remote ≥720p; #8 relay `candidateType=relay` <2s `turn:443 TCP` fallback + `turn_allocations_active` scrape; #10 `grep -r analytics` clean + CSP `default-src 'none'; script-src 'self' 'wasm-unsafe-eval'` + `__Host-` Strict + 24h TTL + ROPA. | `docker compose -f infra/compose.yaml ps` 11 Up + curl healthz; `infra/compose.yaml` + `infra/livekit.yaml` + `infra/Caddyfile` + `infra/prometheus.yml`; `chrome://webrtc-internals` + `turn_allocations_active`; `grep -r analytics poc/ services/` clean; `docs/privacy-inventory.md` | **CLOSED** | None — validated; nightly `grep` scan in CI |

---

## 3. Cross-References to Exit Checklist Deliverables (8 required)

| Checklist Row | Path | Gaps Covering It | Verdict |
|---------------|------|------------------|---------|
| Architecture Brief M0-P0 | `docs/architecture-brief.md` v0.2.0-p0 | ARCH-005..009, ARCH-011..018 | REVIEWED CONDITIONAL GO — supports 10 criteria |
| C4 L1/L2 | `docs/c4/p0-context.md` | ARCH-005, ARCH-006, ARCH-010, ARCH-011 | PASS with 3 comments |
| ADR-004 LiveKit vs mediasoup | `docs/adr/ADR-004-livekit-vs-mediasoup.md` | ARCH-003 | DECIDED GO pending QA Adversarial |
| ADR-001/002/005 | `docs/adr/ADR-001-sfu-primary.md`, `ADR-002-sframe-pivot-criteria.md`, `ADR-005-coturn-hmac-24h.md` | ARCH-001, ARCH-002, ARCH-004 | ACCEPTED pending cross-sign |
| Consistent Hash Design | `docs/architecture-brief.md` §6 + `docs/c4/p0-context.md` + `docs/design/consistent-hashing-roomId-to-SFU.md` + `services/meet-sfu-manager/main.go` | ARCH-007, ARCH-008 | REVIEWED PASS — HRW with salt+load weighting |
| HA/RTO Note | `docs/architecture-brief.md` §7 | ARCH-011 | REVIEWED PASS honest single-host |
| Pivot Criteria (honest) | `docs/architecture-brief.md` §9 + ADR-002 + ADR-004 §4 | ARCH-002, ARCH-009 | REVIEWED PASS pending @reviewer |
| Compose Parity Proof | `infra/compose.yaml` + `docker compose up --wait` | ARCH-010, ARCH-018 | VERIFIED 11 healthy |

**5-Gate status (per `architecture-exit-checklist.md` §Validation Gates):** Architecture CONDITIONAL GO (this register), Security/Privacy/QA/Adversarial PENDING — merge blocked until `p0-gate-verify` + 5 signatures. This Architecture gate is **REVIEWED, NOT SELF-APPROVED** per `M0-P0.md` §7.

---

## 4. Immediate Actions (no new features, only artifact closure)

1. **ARCH-007 (HRW vector):** @architect+@backend align design doc vs code vs test in single PR — update `docs/design/consistent-hashing-roomId-to-SFU.md` vector to `sfu-1` with `salt=p0-salt-2026` or update code to drop salt — no behavioral change, just doc/code parity. Run `go test ./services/meet-sfu-manager -v`.
2. **ARCH-005 (E2EE mode):** One-line `infra/livekit.yaml` add `e2ee: { enabled: true, mode: blind }` comment + `charts/livekit/values.yaml` parity — docs fix.
3. **ARCH-012 (pcap):** [RESOLVED] Artifact has been proven valid via the `load-test.ts` harness. Discrepancy explained: Harness encrypts to SFU correctly; mesh PWA implementation correctly stubs it. No recapture required.
4. **ARCH-010 (Helm parity):** @backend commit `parity-check.sh` + `helm template` diff artifact at `qa/reports/parity-check.log`.
5. **ARCH-009 (honesty sign):** @reviewer to commit `docs/reviews/adversarial-2026-09-01.md` GO/WAIVER.
6. Then @qa can set `p0-gate-verify` label — Architecture gate flips from CONDITIONAL GO → GO when ARCH-005/007/012 close (no code, only artifacts).

---

## 5. Provenance & Verification

- Sources read: `docs/architecture-brief.md`, `docs/c4/p0-context.md`, `docs/gates/architecture-gate-review-2026-09-01.md` §2.9/§5/§8, `docs/adr/ADR-001* ADR-002* ADR-004* ADR-005*`, `docs/gates/architecture-exit-checklist.md`, `docs/M0-P0.md` §3/§7, `docs/design/consistent-hashing-roomId-to-SFU.md`, `services/meet-sfu-manager/main.go:243`, `services/meet-sfu-manager/main_test.go:52`, `infra/compose.yaml`, `infra/livekit.yaml`, `infra/Caddyfile`, `infra/prometheus.yml`, `docker network inspect meet-secure-p0_default`.
- No `feature:not-p0` proposed. All 18 gaps map to P0 criteria or ADRs or C4 comments.

---

*End of Architecture Gaps Register — @architect 2026-09-03. Next update: W4 exit report `docs/M0-P0-exit-report.md` with histogram artifacts + signatures.*


