# M0-P0 Risk Register — CONVERTED TO DECISIONS 2026-09-03

Track identified risks for the M0-P0 milestone that require mitigation strategies. This register complements QA_Audit_Report.md (amber gaps) and architecture-brief.md pivot criteria. **Freeze clause:** No mitigation may start frozen features (breakouts/polls etc remain frozen). Burn-down tracked daily RAG.

> **STOP cataloging — CONVERTED TO DECISIONS 2026-09-03.** Every risk below now has **Decision Date (2026-09-02..04)**, **Closure Criterion (numeric + artifact)**, **Recommendation (GO/NO-GO/WAIVER)**, **DecisionID**, and **Status**. Full 35-risk register with owners, deadlines, evidence, escalation is at `docs/gaps/decision-register.md` (D-001..D-035). This file retains the 5 P0 risks as the authoritative source for those 5, now decision-gated.

| # | Risk | Description | RAG | Trigger / Indicator | Mitigation | Contingency / Pivot | Owner | Detection Method | Artifact to Prove | W1 / W2 / W3 / W4 Milestones | Decision Date | Closure Criterion (numeric + artifact) | Recommendation | DecisionID | Status |
|---|------|-------------|-----|----------------------|------------|----------------------|-------|------------------|-------------------|------------------------------|---------------|----------------------------------------|---------------|------------|--------|
| 1 | Safari SFrame Incompatibility | Safari on macOS/iOS PWA lacks full WebCodecs/SFrame support, risking E2EE degradation or fallback to DTLS. | **R**ed (high likelihood, critical impact) | Absence of SFrame ciphertext in Wireshark capture on Safari; UI shield "E2EE · 3-layer relay" appears; client logs SFrame unavailable. | • Implement blind‑forward 3‑layer relay for ≤20p (Last‑N=9, ~10‑12 Mbps down, 80 % overhead).<br>• Add UI warning ⚠️ "E2EE · 3-layer relay" when SFrame unavailable.<br>• Polyfill WebCodecs via offscreen canvas fallback.<br>• Daily automated Safari headless smoke test (playwright). | If SFrame fails on Safari for >2 participants, pivot to non‑E2EE SFU mesh ≤5 participants; disable E2EE badge and show "Relay‑only" mode. | @webrtc (SFrame) / @frontend (UI shield) | Histogram `sframe-safari-fallback-rate` (target ≤5% of sessions); Wireshark proof capture; Chrome://webrtc-internals SFrame flag. | `qa/reports/sframe-safari-compatibility.html`; link to criterion **#1** (4‑browser matrix) and **#4** (SFrame RFC9605). | W1: Draft risk + trigger definition.<br>W2: Implement 3‑layer relay & UI warning.<br>W3: Add automated Safari smoke test.<br>W4: Freeze‑clause review; if pivot triggered, document decision. | **2026-09-02** | **Closure:** `sframe-safari-fallback-rate ≤5%` over 100 sessions; `qa/reports/wireshark-livekit-sframe.pcapng` packets>0 on bridge `meet-secure-p0_default`, `tshark -Y "rtp && sframe"` KID visible no plaintext `0x000001`; `qa/reports/sframe-safari-compatibility.html` 4-browser PASS. | **GO with WAIVER** — Ship blind-forward 3 layers Last-N=9 + shield `E2EE · 3-layer relay` + ⚠️ DTLS-only warning; fallback mesh ≤5p non-E2EE SFU if >2 Safari participants fail. Never silent. | **D-001** | **A** |
| 2 | TURN Cost Escalation | Coturn relay with HMAC 24h TTL may incur unexpected bandwidth cost if relay usage spikes beyond projected 20 p × 10 min rooms. | **A**mber (moderate likelihood, moderate impact) | Prometheus `turn_allocations_active` exceeds 80% of allocated ports; TURN traffic >5 GB/day in logs; `LIVEKIT_TURN_COST` alert. | • Enforce HMAC TTL 86400s and per‑room nonce reset.<br>• Rate‑limit allocate requests via Redis key `turn:alloc:{roomId}` TTL 5 min.<br>• Monitor `coturn_relay_bytes_total`; set alert at 4 GB/day.<br>• Use iptables DROP test (force `iceTransportPolicy: relay`) in CI to validate fallback. | If TURN cost threshold breached, automatically switch to SFU‑only media (no relay) and surface "Using direct SFU – no TURN" badge; increase TURN secret rotation frequency. | @security (HMAC/TTL) / @ops (coturn ops) | Metric `turn_allocations_active` p95 ≤ 60% of max; histogram `turn_relay_latency_ms` p95 ≤ 200 ms. | `qa/reports/turn-cost-metrics.json`; link to criterion **#8** (TURN relay HMAC 24h). | W1: Define cost thresholds & alerts.<br>W2: Deploy Redis rate‑limit logic.<br>W3: Run cost‑stress load test (50 rooms, 20p).<br>W4: Freeze‑clause validation; no new TURN features until gate pass. | **2026-09-02** | **Closure:** `turn_allocations_active` p95 ≤60% max; `turn_relay_latency_ms` p95 ≤200ms; `coturn_relay_bytes_total` <5GB/day alert 4GB; allocation <2s; artifact `qa/reports/turn-cost-metrics.json` + Prometheus scrape. | **GO** — Enforce TTL86400 + Redis 5m rate-limit + alert; fallback badge `Using direct SFU – no TURN` if >80% ports. | **D-002** | **A** |
| 3 | Client Decrypt Overload | 20‑participant simultaneous decryption of SFrame ciphertext may exceed client CPU ≤70% budget, causing jitter. | **Y**ellow (low‑medium likelihood, high impact) | Client CPU% >70% in performance profiling; frame drop rate >5% in local monitor; Opus/VP9 decode latency >40 ms. | • Optimize SFrame worker: reuse `VideoFrame`, offload to WebAssembly 150KB async path.<br>• Limit simultaneous decoders to Last‑N=9; discard stray streams.<br>• Implement CPU‑budget throttling: pause decryption if p95 > 65% over 30 s.<br>• Profile on reference devices (Chrome 127, Edge 127, Firefox 128, Safari 17.4). | If decrypt overload detected on >15% of clients, automatically downgrade to DTLS‑only (no E2EE) and show "Performance mode – reduced security" banner; route mesh ≤5 participants. | @frontend (WebWorker) / @security (SFrame params) | Histogram `client-decrypt-cpu-p95` ≤ 500 ms p95 across 20 trials; metric `frame-drop-rate` ≤ 2%. | `qa/reports/client-decrypt-cpu.json`; link to criterion **#2** (20p ×10min stable) and **#3** (LiveKit 1.25 SFU). | W1: Add CPU‑budget instrumentation.<br>W2: Deploy optimized SFrame worker build.<br>W3: Run 20‑p client stress test on each browser.<br>W4: Freeze‑clause check; if downgrade triggered, record pivot. | **2026-09-03** | **Closure:** CPU ≤70% p95 on ref devices; `frame-drop-rate ≤2%`; decode latency ≤40ms; `qa/reports/client-decrypt-cpu.json` p95 ≤500ms across 20 trials under 20p; SFU CPU <70% 2vCPU. | **GO with WAIVER** — WASM 150KB + VideoFrame recycle + Last-N=9 + throttle pause >65% 30s; fallback DTLS banner + mesh ≤5p if >15% clients overload. | **D-003** | **A** |
| 4 | Lighthouse Regressions | Bundle size or performance CI (≥95 perf/accessibility/best‑practices) may regress after SFrame/WASM changes. | **G**reen (initially, but watch) | Lighthouse CI score <95 on any of {perf, a11y, best‑practices, SEO}; TBT >200 ms; CLS >0.1; bundle gz >120 kB. | • Enforce `wasm-pack build` size ≤150 KB (gzip) and run before each merge.<br>• Use Workbox GenerateSW with cache-first for static assets.<br>• Daily Lighthouse run on 4‑browser matrix (slow‑4G).<br>• Add `npm run lint` + size‑check gate in CI. | If regression detected, revert WASM change, run `npm run wasm:build` with `--size‑opt`, and re‑measure; if still >120 kB, temporarily disable SFrame worker and fallback to DTLS-only until next sprint. | @frontend (bundle optimization) / @qa (Lighthouse CI) | Lighthouse CI histogram: perf ≥95, a11y ≥95, best‑practices ≥95, TBT <200 ms, CLS 0, gzipped bundle <120 kB. | `qa/reports/lighthouse/` per‑browser JSON files; link to criterion **#9** (Lighthouse ≥95 bundle <120kB gz + WASM 150KB async). | W1: Add size‑check gate to CI pipeline.<br>W2: Implement automated daily Lighthouse run.<br>W3: Fix any failing metrics.<br>W4: Freeze‑clause sign‑off; no new asset landings until gate pass. | **2026-09-03** | **Closure:** Lighthouse perf ≥95 a11y≥95 best≥95 PWA installable TBT<200ms CLS 0 bundle<120kB gz WASM 150KB async + integrity; artifact `qa/reports/lighthouse/*.json` 4×CPU Slow4G. | **GO** — Gate wasm ≤150KB gz + bundle <120kB gz + Workbox + daily Lighthouse; fallback revert + size-opt + DTLS-only if still >120kB. | **D-004** | **A** |
| 5 | Redis Scaling | Pub/sub `signal:{roomId}` and presence `presence:{roomId}:{hash}` TTL 24h may bottleneck under load of 50 rooms × 20 participants. | **R**ed (high likelihood, high impact) | Redis memory usage >80% of allocated 8 GB; `redis_clients_pending_write` > 1000; heartbeat TTL extensions observed. | • Partition keys by room hash; shard across Redis replicas if needed.<br>• Enforce TTL 24h with hourly GC job; add `maxmemory-policy allkeys-lru`.<br>• Monitor `used_memory_human` and `evicted_keys` via Prometheus.<br>• Load‑test with 50 rooms × 20p using `tsx scripts/load-test.ts`. | If Redis exceeds 90% memory, failover to secondary replica, enable read‑only mode, and degrade signalling to in‑process in‑memory fallback; display "Signalling delay" banner. | @backend (Redis schema) / @ops (Redis ops) | Metric `redis_memory_percent` p95 ≤ 70%; `redis_eviction_total` = 0. | `qa/reports/redis-metrics.json`; link to criterion **#3** (LiveKit 1.25 single SFU via Compose + K8s Helm parity) and **#2** (20p ×10min stable). | W1: Define Redis sharding plan & GC script.<br>W2: Deploy Prometheus alerts.<br>W3: Run load‑test 50 rooms × 20p, verify thresholds.<br>W4: Freeze‑clause review; no new room‑scale features until gate pass. | **2026-09-04** | **Closure:** `redis_memory_percent` p95 ≤70%; `redis_eviction_total`=0; 50 rooms×20p stable ≥10min no eviction; artifact `qa/reports/redis-metrics.json` + `redis-cli info memory`. | **GO** — Partition + TTL24h hourly GC + maxmemory LRU + Prometheus `used_memory_human`/`evicted_keys`; fallback secondary replica + in-mem queue + banner if >90%. | **D-005** | **A** |

## Burn‑Down Tracker (Daily RAG)

| Date | Risk 1 | Risk 2 | Risk 3 | Risk 4 | Risk 5 | Overall RAG |
|------|--------|--------|--------|--------|--------|-------------|
| YYYY‑MM‑DD | 🟢/🟡/🟠/🔴 | … | … | … | … | 🟢/🟡/🟠/🔴 |

*Update each day by the owner; if any risk moves to 🔴, trigger gate review per `docs/gates/architecture-exit-checklist.md`.

## Links to M0-P0 Criteria & Gate Checklist

- **Criterion #1** – 4‑browser matrix: Risk 1 (Safari SFrame) and Risk 5 (Redis scaling indirectly affect matrix stability).  
- **Criterion #2** – 20 p × 10 min stable: Risk 3 (client decrypt overload) and Risk 5 (Redis scaling).  
- **Criterion #4** – SFrame RFC9605 ciphertext (Wireshark proof): Risk 1 & Risk 3.  
- **Criterion #8** – TURN relay HMAC 24h: Risk 2.  
- **Criterion #9** – Lighthouse ≥95 bundle <120 kB gz + WASM 150 KB async: Risk 4.  
- **Gate checklist** – `docs/gates/architecture-exit-checklist.md` must pass before any mitigation is considered "released".

*All mitigations respect the **freeze clause**: no frozen feature (breakouts, polls, whiteboard, virtual bg, captions, recording, webinar, anon capability links, P2P↔SFU handoff, WebTransport) may be un‑ frozen during W1‑W4.*